<Window x:Class="KataevWPF.KataevProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
        Height="380" Width="480" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Продукт" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Наименование:" VerticalAlignment="Center" Margin="0,8,0,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtName" Margin="8" Height="28"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Артикул:" VerticalAlignment="Center" Margin="0,8,0,8"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtArticle" Margin="8" Height="28"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Тип:" VerticalAlignment="Center" Margin="0,8,0,8"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtType" Margin="8" Height="28"/>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Width="110" Height="36" Margin="0,0,10,0" Click="BtnSave_Click" Background="#4CAF50" Foreground="White" FontWeight="SemiBold"/>
            <Button Content="Отмена" Width="110" Height="36" Click="BtnCancel_Click" Background="#F44336" Foreground="White" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>